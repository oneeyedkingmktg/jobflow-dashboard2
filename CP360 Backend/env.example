# ============================================================================
# JobFlow Backend - Environment Variables
# ============================================================================

# Database Connection (from Railway)
DATABASE_URL=postgresql://postgres:password@host:5432/database

# JWT Secret (generate a random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# API Key Encryption (generate a random string)
ENCRYPTION_KEY=your-encryption-key-for-api-keys-change-this

# Server Configuration
PORT=3001
NODE_ENV=production

# CORS Origins (comma-separated)
CORS_ORIGINS=https://app.coatingpro360.com,https://app2.epoxyprofitformula.com,http://localhost:5173


# ============================================================================
# How to generate secure secrets:
# ============================================================================
# Run these commands in terminal:
#
# For JWT_SECRET:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# For ENCRYPTION_KEY:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ============================================================================
